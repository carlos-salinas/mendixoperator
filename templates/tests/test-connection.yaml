apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mendixoperator.fullname" . }}-test-connection"
  labels:
    {{- include "mendixoperator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mendixoperator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
