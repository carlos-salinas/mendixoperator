apiVersion: v1
kind: Service
metadata:
  name: {{ include "mendixoperator.fullname" . }}
  labels:
    {{- include "mendixoperator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mendixoperator.selectorLabels" . | nindent 4 }}
